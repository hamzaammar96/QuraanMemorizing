rules_version = '2';

// قواعد أمان Firestore: كل مستخدم يقرأ ويكتب مستنده فقط.
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
